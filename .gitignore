# Предыдущая версия лендинга — хранится локально, в репозиторий не идёт.
# Внутри лежит собственный .git со всей историей старой версии.
ver. 1/

# Шрифты, которые tools/generate-og.py скачивает себе в кэш.
# Не коммитим: скрипт достаёт их сам при первом запуске.
tools/.fonts/

# OS
.DS_Store
Thumbs.db
